f=open("Para.txt") 
de=f.readline() # Reads the first line of the file
print(de)
de=f.readline() # Reads the third line of the file
print(de)                        
de=f.readline() # Reads the fourth line of the file
print(de)                        
de=f.readline() # Reads the five line of the file and so on....
print(de)                        
f.close()